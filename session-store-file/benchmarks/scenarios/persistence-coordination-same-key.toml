name = "persistence-coordination-same-key"
group = "persistence"
command = "coordination"
description = "Concurrent same-key coordination using production inspect operations"
primary_metric = "operations_sec"
higher_is_better = true
requires_broker = false

[args]
concurrency = 8
operations = 100
different_keys = false

[quality]
min_success_rate = 1.0
min_measured_runs = 3
max_primary_cv_pct = 30.0
max_primary_mad_pct = 20.0
max_relative_ci_width_pct = 40.0
